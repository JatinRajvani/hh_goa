# Requirements Split Integration Tasks

- [x] Create `requirements-dev.txt` with full dependencies for local development
- [x] Remove `faiss-cpu`, `sentence-transformers`, and `datasets` from `requirements.txt`
- [x] Update walkthrough / docs to explain local installation vs production deployment
